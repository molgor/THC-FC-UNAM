# JupyterLab with Poetry

## Setup

1. Install Poetry (if needed):
   ```bash
curl -sSL https://install.python-poetry.org | python3 -
```

2. Install dependencies:
   ```bash
poetry install
```

3. Launch JupyterLab:
   ```bash
poetry run jupyter lab
```